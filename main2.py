class pe:
    def twosum(self,numbers,target):
       lookup = {}
        for i, num in enumerate(num):
            if target - num in lookup:
               return (lookup[target-num], i lookup[num] = i
walue = int(input("enter sum for whichh you want to make this search")
print("index=%d, index2=%d") %
pe().twosum((10,20,30,40,50,60,70), value))

        